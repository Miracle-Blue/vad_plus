name: vad_plus
description: "A new Flutter FFI plugin project."
version: 0.0.1

homepage: https://github.com/Miracle-Blue/vad_plus
repository: https://github.com/Miracle-Blue/vad_plus
issue_tracker: https://github.com/Miracle-Blue/vad_plus/issues

platforms:
  android:
  ios:
  macos:

environment:
  sdk: ^3.10.4
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  ffi: ^2.1.3
  ffigen: ^13.0.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        ffiPlugin: true
      ios:
        ffiPlugin: true
      macos:
        ffiPlugin: true
